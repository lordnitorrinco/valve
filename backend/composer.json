{
    "name": "evolve/admission-api",
    "description": "API backend for Evolve Academy admission form",
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "classmap": [
            "app/"
        ]
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "test:coverage": "phpunit --colors=always --coverage-text"
    }
}
