# Test runner container for PHPUnit backend tests.
# Includes Composer for dependency installation.

FROM php:8.3-cli-alpine

# Install extensions: pdo_mysql + curl (same as prod) + pcov for coverage
RUN apk add --no-cache libcurl curl-dev autoconf g++ make linux-headers \
    && docker-php-ext-install pdo pdo_mysql curl \
    && pecl install pcov && docker-php-ext-enable pcov \
    && apk del autoconf g++ make linux-headers

# Install Composer from official image
COPY --from=composer:2 /usr/bin/composer /usr/bin/composer

WORKDIR /app

# Copy entire backend (source + tests + config)
COPY . .

# Install PHPUnit and generate autoloader
RUN composer install --no-interaction --prefer-dist

# Run tests by default
CMD ["vendor/bin/phpunit", "--colors=always", "--coverage-text"]
